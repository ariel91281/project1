
#include <stdio.h>

int stock(int arr, arr size)
{
	int i;
	int max;
	int min;
	int profit[3];
	min=max=arr[0];
	for(i=1; i<size ; i++)
	{
		if(arr[i]>max)
			max = arr[i];
		else 
			min = arr[i];
	}

	profit[0] = max-min;
	profit[1] = min;
	profit[2] = profit;
}

int main()
{
	stock(10,5);
}
